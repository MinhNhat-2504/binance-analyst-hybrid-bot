"""Fail-closed execution and public market-data collection utilities.

Nothing in this package submits production orders.  The only supplied order runner is
explicitly testnet-only and requires separate testnet credentials plus a manual kill-switch
release.
"""
